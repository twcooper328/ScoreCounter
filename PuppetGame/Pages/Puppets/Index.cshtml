@page
@model PuppetGame.Pages.Puppets.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Hat[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Hat[0].Energy)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Hat[0].Pot)
            </th>
            
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Hat) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Energy)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Pot)
            </td>
            
            <td>
                <a asp-page="./Edit" asp-route-id="@item.HatID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.HatID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.HatID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
